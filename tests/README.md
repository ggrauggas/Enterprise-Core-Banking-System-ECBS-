# Tests

Tests de integración transversales del sistema (Fase 14). Los tests
unitarios viven junto a cada componente (`backend/src/test`,
`frontend/src/**/*.test.tsx`, tests de programas COBOL en `cobol/`).
